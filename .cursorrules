# Todo App 开发规范

## 项目概述
这是一个现代化的待办事项管理应用，采用Apple风格设计，支持暗色模式、中英文切换等高级功能。

## 技术栈
- HTML5 (语义化标签，良好的可访问性)
- CSS3 (CSS变量、Grid/Flexbox布局、CSS动画)
- 原生JavaScript ES6+ (模块化组织，无框架依赖)
- LocalStorage (数据持久化)

## 代码规范

### HTML
- 使用语义化标签 (`<main>`, `<section>`, `<article>`, `<header>`, `<footer>`)
- 良好的可访问性 (`aria-label`, `aria-live`, `aria-hidden`)
- 国际化支持 (`data-i18n`, `data-i18n-placeholder`, `data-i18n-title`)
- 响应式设计 (使用 `clamp()` 函数和媒体查询)

### CSS
- CSS变量定义主题颜色和尺寸
- 玻璃拟态效果 (`backdrop-filter: blur()`)
- BEM命名规范 (可选，保持一致性)
- 响应式断点:
  - 桌面: > 1024px
  - 平板: 640px - 1024px
  - 手机: < 640px

### JavaScript
- JSDoc注释 (类型标注，函数说明)
- 模块化组织 (ES6 Modules)
- 错误处理完善 (try-catch, 用户友好提示)
- 事件监听器组织良好

## 设计系统

### 颜色系统
- 主色调: `#1677ff` (蓝色)
- 成功色: `#12b76a` (绿色)
- 警告色: `#f79009` (黄色)
- 错误色: `#f04438` (红色)
- 文本层级: `--text-1` (主要), `--text-2` (次要), `--text-3` (辅助)

### 形状系统
- 大圆角: `24px` (`--radius-lg`)
- 中圆角: `16px` (`--radius-md`)
- 小圆角: `12px` (`--radius-sm`)

### 阴影系统
- 常规阴影: `0 10px 30px rgba(14, 25, 45, 0.09)`
- 大阴影: `0 16px 40px rgba(14, 25, 45, 0.12)`
- 小阴影: `0 4px 12px rgba(14, 25, 45, 0.05)`

## 功能模块

### 核心模块
1. `storage-manager.js` - 数据持久化
2. `todo-manager.js` - 待办事项业务逻辑
3. `theme-manager.js` - 主题管理
4. `i18n-manager.js` - 国际化管理
5. `ui-manager.js` - 界面渲染和交互

### 数据模型
```javascript
{
  id: string,           // 唯一标识
  title: string,        // 任务标题
  description: string,  // 任务描述
  completed: boolean,   // 完成状态
  priority: enum,       // 优先级: low/medium/high
  dueDate: date,        // 截止日期
  tags: array,          // 标签数组
  category: string,     // 分类
  createdAt: date,      // 创建时间
  updatedAt: date,      // 更新时间
}
```

## 开发流程

### 添加新功能
1. 在对应模块文件中添加功能代码
2. 如果需要新UI组件，在 `ui-manager.js` 中添加
3. 如果需要新数据字段，更新 `Todo` 类
4. 更新国际化文本 (在 `i18n-manager.js` 中)
5. 测试功能完整性

### 修改样式
1. 优先修改CSS变量
2. 使用现有设计系统组件
3. 确保响应式适配
4. 测试暗色/亮色模式

### 修复Bug
1. 复现问题并记录步骤
2. 定位问题所在模块
3. 编写修复代码
4. 测试修复效果
5. 添加防止回归的测试 (可选)

## 国际化

### 翻译文件结构
```javascript
const TRANSLATIONS = {
  zh: {
    key: "中文文本"
  },
  en: {
    key: "English text"
  }
};
```

### 添加新翻译
1. 在 `i18n-manager.js` 中添加翻译键值对
2. 在HTML中添加 `data-i18n` 属性
3. 测试中英文切换

## 测试要求

### 功能测试
- 任务CRUD操作正常
- 筛选排序功能正常
- 主题切换正常
- 语言切换正常
- 数据持久化正常

### 兼容性测试
- Chrome/Firefox/Safari/Edge
- 移动端浏览器
- 不同屏幕尺寸

### 性能测试
- 大量任务时的渲染性能
- 本地存储读写性能
- 动画流畅度

## 部署说明

### 生产环境
1. 压缩CSS和JavaScript文件
2. 添加缓存策略
3. 配置Service Worker (可选)
4. 添加分析代码 (可选)

### 开发环境
1. 本地HTTP服务器
2. 启用开发工具
3. 禁用缓存以便调试

## 文档要求

### 代码注释
- 每个函数有JSDoc注释
- 复杂逻辑有行内注释
- 公开API有详细说明

### 用户文档
- README.md 项目说明
- 功能使用指南
- 常见问题解答

## 版本管理

### 提交消息格式
```
feat: 添加新功能
fix: 修复Bug
docs: 文档更新
style: 代码样式调整
refactor: 代码重构
test: 测试相关
chore: 构建过程或辅助工具变动
```

### 分支策略
- `main` - 生产环境代码
- `develop` - 开发分支
- `feature/*` - 功能分支
- `fix/*` - Bug修复分支

## 注意事项

1. **数据安全**: 所有数据存储在用户本地，不上传服务器
2. **用户体验**: 操作有明确反馈，动画流畅
3. **可访问性**: 支持键盘导航，屏幕阅读器友好
4. **性能**: 优化渲染性能，避免内存泄漏
5. **维护性**: 代码模块化，便于扩展和维护

## 紧急情况处理

### 数据丢失
1. 检查localStorage是否被清除
2. 尝试从备份恢复
3. 提示用户重新导入数据

### 功能异常
1. 检查浏览器控制台错误
2. 验证模块依赖关系
3. 回滚到稳定版本

---

**最后更新**: 2026-03-31
**维护者**: Todo App开发团队